# --- Core API (preserved from the original backend) ---
fastapi==0.137.2
uvicorn==0.49.0
SQLAlchemy==2.0.51
alembic==1.18.4
psycopg2-binary==2.9.12
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
python-multipart==0.0.32
python-jose==3.5.0
bcrypt==4.0.1
PyPDF2==3.0.1
razorpay==2.0.1
email-validator==2.3.0

# --- OpenAI ---
# Primary AI backend for emotion/character/shot/BGM/scene-importance/
# cost-estimation/storyboard analysis. See app/services/openai_service.py.
openai==2.44.0

# --- AI / NLP pipeline (local fallback models) ---
# CPU-only torch is strongly recommended for deployment (much smaller image,
# no CUDA deps needed since this runs on a CPU-only Render/Fly/EC2 instance).
# Install with the CPU wheel index, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# This requirements.txt lists the version pin only; see backend/README.md
# for the exact install command used in the Dockerfile.
torch==2.12.1
transformers==4.57.0
spacy==3.8.7

# en_core_web_trf is installed separately via:
#   python -m spacy download en_core_web_trf
# (spaCy model packages are published as their own pip-installable wheels,
# not as a regular PyPI dependency you can pin here.)
